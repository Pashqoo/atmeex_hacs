# Atmeex Cloud Integration for Home Assistant

Интеграция для управления бризерами Atmeex (AirNanny) через Home Assistant с использованием Atmeex Cloud API.

## ✨ Особенности

- 🔄 Поддержка нескольких устройств
- 🌡️ Управление температурой и режимами работы (10-30°C)
- 💨 Контроль скорости вентиляции (1-7)
- 🌬️ Управление положением заслонки (открыта/смешанный/закрыта)
- 💧 Установка целевой влажности (0-100%)
- 🤖 Автоматический режим работы
- 🌙 Ночной режим
- ❄️ Режим охлаждения
- 🔍 Индикатор состояния (онлайн)
- ⚡ Автоматическое обновление состояния устройств
- 🔐 Безопасное хранение токенов доступа

## 📋 Требования

- Home Assistant 2024.1 или новее
- Активная учетная запись Atmeex
- Устройства должны быть добавлены в ваш аккаунт через мобильное приложение Atmeex

## 🚀 Установка

### Через HACS (рекомендуется)

1. Установите [HACS](https://hacs.xyz/) если еще не установлен
2. Перейдите в **HACS** → **Интеграции**
3. Нажмите три точки (⋮) → **Пользовательские репозитории**
4. Добавьте:
   - **URL:** `https://github.com/Pashqoo/atmeex_hacs`
   - **Тип:** Integration
5. Найдите **"Atmeex cloud"** в списке интеграций
6. Нажмите **Установить**
7. Перезагрузите Home Assistant

### Ручная установка

1. Скопируйте папку `custom_components/atmeex_cloud` в папку `/config/custom_components/` вашего Home Assistant
2. Перезагрузите Home Assistant

## ⚙️ Настройка

1. Перейдите в **Настройки** → **Устройства и службы**
2. Нажмите **Добавить интеграцию**
3. Найдите и выберите **Atmeex cloud**
4. Введите:
   - **Email:** ваш email от учетной записи Atmeex
   - **Password:** ваш пароль от учетной записи Atmeex
5. Нажмите **Отправить**

Интеграция автоматически:
- Подключится к API Atmeex
- Обнаружит все устройства в вашем аккаунте
- Создаст entities для управления каждым устройством

## 📱 Создаваемые Entities

Для каждого устройства создаются:

### Climate Entity (`climate.atmeex_*`)
- Управление температурой и режимами работы
- Режимы: `heat`, `fan_only`, `off`
- Управление температурой (10-30°C)
- Управление скоростью вентиляции (1-7)

### Fan Entity (`fan.atmeex_*`)
- Управление вентиляцией (включить/выключить)
- Установка скорости вентиляции (0-100%)

### Sensor Entities (`sensor.atmeex_*_*`)
- **Humidity Target** - текущая целевая влажность (%)
- **Fan Speed** - текущая скорость вентилятора (1-7)
- **Damper Position** - текущее положение заслонки (Open/Mixed/Closed)
- **Firmware Version** - версия прошивки устройства

> **Примечание:** Датчики CO2, Temperature, Humidity удалены, так как они доступны только когда устройство онлайн через WebSocket, а не через REST API. Эти данные можно получить только если устройство активно подключено через WebSocket (например, если открыто мобильное приложение Atmeex).

### Binary Sensor Entities (`binary_sensor.atmeex_*_*`)
- **Online** - статус подключения к облаку (включен = онлайн)

> **Примечание:** Датчик "No Water" удален, так как он зависит от `condition`, который недоступен через REST API.

### Select Entity (`select.atmeex_*_damper`)
- Управление положением заслонки
- Варианты: `open` (открыта), `mixed` (смешанный), `closed` (закрыта)
- Используется для пассивного проветривания

### Number Entity (`number.atmeex_*_humidity_target`)
- Установка целевой влажности
- Диапазон: 0-100%
- Шаг: 1%

### Switch Entities (`switch.atmeex_*_*`)
- **Auto Mode** - автоматический режим работы
- **Night Mode** - ночной режим
- **Cool Mode** - режим охлаждения

## 🔧 Использование

### Управление через UI

1. Перейдите в **Настройки** → **Устройства и службы**
2. Найдите ваше устройство Atmeex
3. Используйте карточки для управления

### Управление через автоматизации

**Пример 1: Управление по времени:**
```yaml
automation:
  - alias: "Включить бризер утром"
    trigger:
      - platform: time
        at: "07:00:00"
    action:
      - service: climate.set_hvac_mode
        target:
          entity_id: climate.atmeex_brizer_1
        data:
          hvac_mode: fan_only
      - service: climate.set_fan_mode
        target:
          entity_id: climate.atmeex_brizer_1
        data:
          fan_mode: "3"
```

**Пример 2: Управление температурой обогрева:**
```yaml
automation:
  - alias: "Включить обогрев по расписанию"
    trigger:
      - platform: time
        at: "18:00:00"
    action:
      - service: climate.set_hvac_mode
        target:
          entity_id: climate.atmeex_brizer_1
        data:
          hvac_mode: heat
      - service: climate.set_temperature
        target:
          entity_id: climate.atmeex_brizer_1
        data:
          temperature: 22
```

**Пример 3: Управление по влажности:**
```yaml
automation:
  - alias: "Включить вентиляцию при высокой влажности"
    trigger:
      - platform: numeric_state
        entity_id: sensor.atmeex_brizer_1_humidity
        above: 70
    action:
      - service: climate.set_hvac_mode
        target:
          entity_id: climate.atmeex_brizer_1
        data:
          hvac_mode: fan_only
```

### Управление через шаблоны

```yaml
service: climate.set_temperature
target:
  entity_id: climate.atmeex_brizer_1
data:
  temperature: 22
```

## 🐛 Устранение неполадок

### Интеграция не устанавливается

- Убедитесь, что используете Home Assistant 2024.1 или новее
- Проверьте, что HACS установлен и обновлен

### Ошибка "no devices found in account"

- Убедитесь, что устройства добавлены в ваш аккаунт через мобильное приложение
- Проверьте правильность email и password

### Устройства не обновляются

- Проверьте логи Home Assistant на наличие ошибок
- Убедитесь, что устройства подключены к интернету
- Перезагрузите интеграцию через **Настройки** → **Устройства и службы** → **Atmeex cloud** → **Перезагрузить**

## 📝 Изменения в версии 0.2.0

### Исправления

- ✅ Исправлена ошибка `AttributeError: 'ConfigEntries' object has no attribute 'async_forward_entry_setup'`
  - Заменен устаревший API на `async_forward_entry_setups` для совместимости с новыми версиями Home Assistant
- ✅ Исправлена проблема с обновлением токенов доступа
  - Используется `entry.data.copy()` для корректного обновления конфигурации
- ✅ Обновлена версия библиотеки `atmeexpy`
  - Изменено с `atmeexpy==0.1.0` на `atmeexpy>=0.1.0` для поддержки новых версий

### Улучшения

- 📦 Поддержка всех версий библиотеки atmeexpy >= 0.1.0

## 👨‍💻 Разработка

### Базируется на

- [atmeex_hacs](https://github.com/anpavlov/atmeex_hacs) от @anpavlov
- [atmeexpy](https://github.com/anpavlov/atmeexpy) - Python библиотека для работы с API Atmeex

### Известные проблемы

- Проект находится в активной разработке
- Сообщения об ошибках приветствуются через Issues

## 📄 Лицензия

Этот проект основан на оригинальном проекте от @anpavlov и продолжается в качестве форка с исправлениями.

## 🤝 Вклад

Сообщения об ошибках и предложения по улучшению приветствуются через [Issues](https://github.com/Pashqoo/atmeex_hacs/issues).

## ⚠️ Отказ от ответственности

Эта интеграция не является официальным продуктом Atmeex и предоставляется "как есть" без гарантий.

